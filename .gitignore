# Large binary artifacts - never commit these
embeddings/*.bin
embeddings/*.vec
checkpoints/*.pt
data/processed/
logs/

# Python
__pycache__/
*.pyc
.venv*/
venv/

# Downloaded Kaggle output bundles (the useful contents are extracted into
# results/, figures/ and checkpoints/ and committed individually)
*.zip

# Hugging Face / dataset caches
.cache/
